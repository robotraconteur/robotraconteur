//This file is automatically generated. DO NOT EDIT!
package com.robotraconteur.testing.TestService3;
import java.util.*;
import com.robotraconteur.*;
public class pixel implements RRNamedArray 
{
    public UnsignedByte r;
    public UnsignedByte b;
    public UnsignedByte g;
    public pixel()
    {
    r = new UnsignedByte((byte)0);
    b = new UnsignedByte((byte)0);
    g = new UnsignedByte((byte)0);
    }
    public UnsignedBytes getNumericArray()
    {
    UnsignedBytes a = new UnsignedBytes(3);
    getNumericArray(a,0);
    return a;
    }
    public void getNumericArray(UnsignedBytes buffer, int offset)
    {
    buffer.value[offset + 0] = r.value;
    buffer.value[offset + 1] = b.value;
    buffer.value[offset + 2] = g.value;
    }
    public void assignFromNumericArray(UnsignedBytes buffer, int offset)
    {
    r = buffer.get(offset + 0);
    b = buffer.get(offset + 1);
    g = buffer.get(offset + 2);
    }
    public static UnsignedBytes getNumericArray(pixel[] s)
    {
    UnsignedBytes a = new UnsignedBytes(3 * s.length);
    getNumericArray(s,a,0);
    return a;
    }
    public static void getNumericArray(pixel[] s, UnsignedBytes a, int offset)
    {
    for (int i=0; i<s.length; i++)
    {
    s[i].getNumericArray(a, offset + 3 * i);
    }
    }
    public static void assignFromNumericArray(pixel[] s, UnsignedBytes a, int offset)
    {
    for (int i=0; i<s.length; i++)
    {
    s[i].assignFromNumericArray(a, offset + 3 * i);
    }
    }
}

